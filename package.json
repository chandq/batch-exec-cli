{
  "name": "batch-exec-cli",
  "version": "1.4.0",
  "description": "Efficiently iterate through directories and execute commands with progress display and parallel execution",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "batch-exec": "bin/batch-exec"
  },
  "scripts": {
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-include='src/**' --test-coverage-exclude='test/**'",
    "lint": "eslint src/ test/"
  },
  "files": [
    "src",
    "bin"
  ],
  "homepage": "https://github.com/chandq/batch-exec-cli#readme",
  "bugs": {
    "url": "https://github.com/chandq/batch-exec-cli/issues"
  },
  "keywords": [
    "batch-exec",
    "batchexec",
    "batch-exec-cli",
    "batch",
    "execute",
    "command",
    "directory",
    "zx",
    "progress",
    "cli"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chandq/batch-exec-cli.git"
  },
  "author": "chandq",
  "license": "MIT",
  "dependencies": {
    "minimist": "^1.2.8",
    "zx": "^8.8.5"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=12.17.0"
  }
}
